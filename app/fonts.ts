import { Inter, Josefin_Sans } from 'next/font/google'

export const text = Inter({
  subsets: ['latin'],
  display: 'swap',
})

export const title = Josefin_Sans({
  subsets: ['latin'],
  display: 'swap',
})
